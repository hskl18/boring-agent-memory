# Invitations

## Retry

When `ClerkInvitationError` reports an expired ticket, issue a new invitation before continuing.

## Retry

When the browser callback is valid, continue through `/portal/accept-token` and preserve the original redirect.
