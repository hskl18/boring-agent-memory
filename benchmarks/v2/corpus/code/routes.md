# Operator symbols

Use `HTTP_409` for a conflicting migration request.
Run `bam update --dry-run` before applying index lifecycle changes.
The `foo::bar` handler owns the `/portal/accept-token` callback.
