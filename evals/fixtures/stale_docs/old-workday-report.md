# Old Workday Report

This old report mentions Workday application tracking, but it predates the
current `postingAvailable:false` expiration rule. It is useful historical
context, not the canonical policy.
