# Codex Usage

Use Boring Agent Memory only when recall is needed.

Suggested rule:

> Query local memory for prior project conventions, then read the cited source file before making claims or edits.

Example:

```bash
bam query "CareerOS judge demo static sanitized" --limit 5 --json
```

